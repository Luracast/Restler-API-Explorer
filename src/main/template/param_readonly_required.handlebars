<td class='code required'>{{name}}: <tag>{{{signature}}}</tag></td>
<td>
    {{#if isBody}}
        <textarea class='body-textarea'  readonly='readonly' placeholder='(required)' name='{{name}}'>{{defaultValue}}</textarea>
    {{else}}
        {{defaultValue}}
    {{/if}}
</td>
<td>{{{description}}}</td>